# MongoDB connection string
# Replace username, password, and cluster host with your own values
DATABASE_URL="mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/saxcorner?retryWrites=true&w=majority"
CLOUDINARY_URL="cloudinary://<api_key>:<api_secret>@<cloud_name>"

# PayPal Configuration (Required for checkout)
# Get these from https://developer.paypal.com/dashboard/applications

# Option 1: PayPal REST API (Advanced)
NEXT_PUBLIC_PAYPAL_CLIENT_ID="your_paypal_client_id"
PAYPAL_CLIENT_SECRET="your_paypal_client_secret"

# Option 2: PayPal Standard Button + IPN (Simple)
# Use your PayPal Business email for Standard Button
NEXT_PUBLIC_PAYPAL_BUSINESS_EMAIL="your-business@email.com"

# PayPal Mode: "sandbox" for testing, "live" for production
NEXT_PUBLIC_PAYPAL_MODE="sandbox"
PAYPAL_MODE="sandbox"

# Base URL for IPN callbacks (required for PayPal Standard)
NEXT_PUBLIC_BASE_URL="http://localhost:3000"

# Optional: override API base when calling from external clients
# NEXT_PUBLIC_API_URL="http://localhost:3000"
